import React from 'react';
import Layout from "../components/Layout";

const Contact = () => {
    const content = <div>This is Contact page</div>;
    return (
        <Layout content={content} />
    );
};

export default Contact;
