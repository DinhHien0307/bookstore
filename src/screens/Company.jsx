import React from 'react';
import Layout from "../components/Layout";

const Company = () => {
    const content = <div>This is Company page</div>;
    return (
        <Layout content={content} />
    );
};

export default Company;
