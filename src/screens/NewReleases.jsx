import React from 'react';
import Layout from "../components/Layout";

const NewReleases = () => {
    const content = <div>This is New Releases page</div>;
    return (
        <Layout content={content} />
    );
};

export default NewReleases;
